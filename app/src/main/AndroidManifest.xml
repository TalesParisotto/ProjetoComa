<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.labengenharia.coma">

    <application
        android:allowBackup="true"
        android:icon="@drawable/mascote_coma"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.RedefinirSenhaActivity"></activity>
        <activity android:name=".activity.EditarDepartamentoActivity" />
        <activity android:name=".activity.DepartamentoActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/Theme.Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.CadastroActivity" />
        <activity
            android:name=".activity.PrincipalActivity"
            android:label="@string/title_activity_principal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.ReceitasActivity"
            android:theme="@style/receitaTheme" />
        <activity
            android:name=".activity.DespesasActivity"
            android:theme="@style/despesaTheme" />
    </application>

</manifest>